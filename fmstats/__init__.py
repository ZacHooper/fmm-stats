"""Query layer over a career store: which copy to read (`store`), the R2-mirrored scout log
(`state`), and the opposition-scouting engine (`scout`). `fmparser/` builds the store; this
package only reads it."""
